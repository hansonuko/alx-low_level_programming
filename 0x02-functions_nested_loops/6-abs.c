#include "main.h"

/**
 * _abs - function that computes absolute value of an integer
 * @int: is th int to be checked
 * Return:the abs of the int.
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
